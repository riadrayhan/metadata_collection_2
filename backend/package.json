{
  "name": "datacollector-backend",
  "version": "3.0.0",
  "description": "Backend for Android Data Collector App — Railway/Vercel deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step needed'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^7.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
